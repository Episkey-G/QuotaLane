# QuotaLane 环境变量配置模板
# 使用方法：cp .env.example .env 并修改相应的值

# ==========================================
# 数据库配置
# ==========================================
# MySQL 数据库连接字符串（Docker 环境）
# 格式：user:password@tcp(host:port)/database?charset=utf8mb4&parseTime=True&loc=Local
MYSQL_DSN=root:root@tcp(mysql:3306)/quotalane?charset=utf8mb4&parseTime=True&loc=Local

# MySQL Root 密码（与 docker-compose.yml 中的 MYSQL_ROOT_PASSWORD 一致）
MYSQL_ROOT_PASSWORD=root

# ==========================================
# Redis 配置（可选，默认使用 mysql:6379）
# ==========================================
# Redis 地址（Docker 环境）
# REDIS_ADDR=redis:6379
# REDIS_PASSWORD=

# ==========================================
# 认证配置（必需 - 生产环境必须修改！）
# ==========================================
# JWT 密钥（用于签发和验证 JWT Token）
# 要求：至少 32 个字符的随机字符串
# 生成方法：openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here-must-be-at-least-32-characters-long

# 数据加密密钥（用于敏感数据加密存储，如 API Key、Token 等）
# 要求：精确 32 个字符（AES-256 要求）
# 生成方法：openssl rand -hex 16
ENCRYPTION_KEY=12345678901234567890123456789012

# ==========================================
# 应用环境配置
# ==========================================
# 应用运行环境（development/production）
QUOTALANE_ENV=development

# ==========================================
# Grafana 配置
# ==========================================
# Grafana 管理员密码
GF_SECURITY_ADMIN_PASSWORD=admin

# ==========================================
# 安全提示
# ==========================================
# ⚠️ 生产环境部署前必须修改以下配置：
# 1. JWT_SECRET - 使用强随机字符串（>= 32字符）
# 2. ENCRYPTION_KEY - 使用32字符随机字符串
# 3. MYSQL_ROOT_PASSWORD - 使用强密码
# 4. GF_SECURITY_ADMIN_PASSWORD - 使用强密码
# 5. QUOTALANE_ENV - 设置为 production
#
# ⚠️ 此文件包含敏感信息，已在 .gitignore 中排除，不要提交到 Git！
